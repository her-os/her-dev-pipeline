# Changelog (Archive)

---

### 2026-04-30 her-web access stop bleeding 发布与额度账本修复

> 摘要：按正常发布流程把 her-web `c7ac600` 发布到 `hersoul.cn`，随后只修 her-web 生产库里的邀请/HerClub 本地额度账本到 1000；没有调用 gateway Admin API，没有改 gateway 实际余额。

commit: `c7ac600dd4d4db5b79bb24c1f5dae73c5129ac68`（her-web main） | 回滚：用 `scripts/her-web/rollback.sh /Users/suyuan/Documents/her-source/her-web previous` 回滚服务；数据修复回滚参考服务器备份目录里的 `user_gateway_before.jsonl` / `gift_credit_before.jsonl`

**发布**：
- PR：`https://github.com/her-os/Her-Web/pull/31`
- service：`her-herweb-a8y5ka`
- domain：`https://hersoul.cn`
- image：`sha256:fc177c2fd9aa8ee81670eac68843d3e697b90749de2480e8720541fe8058b62c`
- release report：`her-web-c7ac600-1777542636`
- metadata：`metadataTrusted=true`

**数据修复**：
- 备份目录：`/home/ubuntu/her-web-release/data-fixes/20260430-her-web-invite-quota-1000-20260430102026`
- 范围：her-web Postgres only
- eligible 邀请码 / HerClub 用户：46
- `user_gateway.quota_granted` 低于 1000 的已存在绑定：15 条，更新到 `500000000`
- 本地 gift credit 账本不足 1000 的用户：13 个，插入差额 grant，总差额 `6379500000`
- 缺 gateway binding 的用户：2 个，只补 web credit 账本，不创建假 gateway binding

**边界**：
- 没有调用 gateway Admin API
- 没有给 gateway 追加余额
- 没有修 gateway 连通性；gateway 缺失/陈旧记录留给下一个 gateway-focused task
- 5 小时限额已确认是滚动数据库窗口，不是定时刷新任务；旧 usage row 超过 5 小时后会在下一次读取时自然掉出窗口

**验证**：
- 发布 postflight：replicas `1/1`，`https://hersoul.cn` HTTP 200，auth endpoint not 403，容器日志无 error/duplicate key
- 数据修复后：eligible 46；已有 gateway 的 44 个用户本地 `quota_granted >= 500000000`；gift ledger under 1000 为 0

---

### 2026-04-30 her-web release fingerprint 复核修正

> 摘要：修正 `deploy.sh` release report 复核时的 running image 取值口径，避免 `release-check.sh` 用 image id、`deploy.sh` 用 tag 导致同一生产状态被误判为 report 过期。

commit: 未提交（共享 skill / 本地流程记录） | 回滚：恢复 `scripts/her-web/deploy.sh` 本次一行口径修正

**改动**：
- `deploy.sh` Step 1b 先取当前 her-web 容器 id，再用 `docker inspect --format '{{.Image}}'` 读取 running image id
- 与 `release-check.sh` 的 fingerprint 输入保持一致：service image + running image id + task id + current.json sha

**验证**：
- `release.sh /Users/suyuan/Documents/her-source/her-web origin/main --mode normal` 不再因 tag/image-id 口径差异误报 `生产状态已变化`
- 本轮 `c7ac600` 真实发布通过 preflight、deploy、postflight

---

### 2026-04-30 local-prod 常用账号与 3002 gateway

> 摘要：local-prod smoke 固化两个本地约定：常用测试账号只从本机私有文件读取；her-web local-prod 必须使用 snapshot gateway `3002`，不要误接 Docker dev gateway `3001`。

commit: 未提交（共享 skill / 本地流程记录） | 回滚：恢复 `SKILL.md`、`references/her-web/local-prod-snapshot.md`、`index.md`、`changelog.md` 的本次改动

**改动**：
- `SKILL.md` 补充：常用本地测试账号文件为 `/Users/suyuan/Documents/her-source/local-prod-snapshot/env/local-test-accounts.md`
- `local-prod-snapshot.md` 增加常用本地测试账号规则：文件权限保持 `600`，明文密码不进 skill、仓库、Trellis 任务、提交记录或对外日志
- `local-prod-snapshot.md` 补充 `3002` gateway 启动命令，并明确 Docker `new-api:3001` 是 her-gateway 本地 dev，不保证和 her-web local-prod env 的 admin token 匹配
- `index.md` 更新 local-prod 文档摘要和 changelog 索引

**边界**：
- 没有把任何明文密码写入 her-ops skill
- 没有改业务仓库代码
- 没有部署、重启或修改生产环境

---

### 2026-04-29 her-web release metadata bootstrap

> 摘要：初始化 her-web 当前生产版本记录，让 `release.sh` 后续能对比 main / suyuan / 生产运行事实。写入的是 metadata，不是部署：没有切镜像、没有重启、没有改数据库。

commit: 未提交（共享 skill / 服务器 metadata 记录） | 回滚：删除服务器 `/home/ubuntu/her-web-release/current.json` 中本次 bootstrap 内容，或恢复到重新 bootstrap 前的空 metadata 状态；不影响当前 Swarm service 镜像

**写入内容**：
- `/home/ubuntu/her-web-release/current.json`
  - `commit=b08acd102643e470be0b893f470ead9b05748f69`
  - `image=sha256:dee5f167d7aef58992808890fa03a70210edea8667e756e920097d8756e76648`
  - `bootstrap=true`
  - `metadataTrusted=false`
  - `requiredHotfixCommits=[39a5df9,5513448]`
- `/home/ubuntu/her-web-release/history.jsonl` 追加同一条记录

**验证**：
- Swarm service 仍是 `1/1 sha256:dee5f167...`
- running container 仍是 `f6bc24aa58fd dee5f167d7ae`
- `current.json` 是合法 JSON
- `release-check` 结果从 `BLOCKED: metadata missing` 变成 `WARN: bootstrap metadata accepted with hotfix proof`
- `release.sh --dry-run` 在接受 report 后可跑完 preflight，并明确停在 `DRY RUN COMPLETE`

**边界**：
- 这不是一次生产部署
- 没有 `docker service update`
- 没有重启服务
- 没有改数据库
- 第一次真实 `release.sh` 发布仍会要求接受 bootstrap WARN；发布成功后新 metadata 才会变成 `metadataTrusted=true`

---

### 2026-04-29 her-web release.sh 发布闸门

> 摘要：把 her-web 生产发布入口从“手动拼 deploy.sh / preflight / postflight”收口到 `release.sh`。新增只读 release report、临时干净 worktree、report hash、main/suyuan/生产运行事实对齐检查、本地和服务器 release lock、最小 rollback 入口，并让低层 A/B/C 脚本默认不能直接切生产。

commit: 未提交（共享 skill / 本地流程记录） | 回滚：删除 `scripts/her-web/release.sh`、`release-check.sh`、`rollback.sh`，恢复 `deploy.sh`、`deploy-preflight.sh`、`deploy-main-digest.sh`、`remote-preview-deploy.sh`、`references/her-web/standard-deploy.md`、`index.md`、`changelog.md`、`/Users/suyuan/.codex/AGENTS.md` 的本次改动

**改动**：
- 新增 `scripts/her-web/release-check.sh`：只读生成 release report，检查实时 `origin/main`、生产 metadata、required hotfix、schema/migration、高风险路径、suyuan 是否有高风险改动
- 新增 `scripts/her-web/release.sh`：唯一生产发布入口，创建临时 worktree，要求 report 确认和 smoke 确认，再调用底层方案 C
- 新增 `scripts/her-web/bootstrap-current.sh`：安全初始化当前生产 metadata，默认 dry-run；真实写入需要 `CONFIRM_BOOTSTRAP=1`
- 新增 `scripts/her-web/rollback.sh`：从 `previous.json` 或 report rollbackTarget 做最小回滚，真实回滚需要 `CONFIRM_ROLLBACK=1`
- `deploy.sh` 改为 release 内部执行器；缺少 `HER_WEB_RELEASE_INTERNAL=1`、`EXPECTED_TARGET_SHA`、`EXPECTED_REPORT_HASH`、`RELEASE_REPORT_PATH` 时默认拒绝真实生产部署
- `deploy.sh` 构建时写入 `org.opencontainers.image.revision` label，并给本地镜像打 `her-web:release-<sha>-<time>` 不可变 tag；部署成功后写 `current.json` / `history.jsonl`
- `deploy-preflight.sh` 改为支持 `EXPECTED_TARGET_SHA` / `REMOTE_ORIGIN_MAIN_SHA`，不再只靠分支名判断；`schema.mysql.ts` 单独变化只 WARN
- `deploy-main-digest.sh` 和 `remote-preview-deploy.sh` 默认禁止直接切生产，必须显式授权变量
- `standard-deploy.md`、`index.md`、`/Users/suyuan/.codex/AGENTS.md` 改为提示 her-web 生产发布只能走 `release.sh`
- `standard-deploy.md` 把旧的直接 `deploy.sh`、方案 A、方案 B 移到「历史路径 / 回滚参考」，保留旧流程细节方便事故回滚和复盘

**边界**：
- 本次只是实现脚本和文档，没有执行生产部署
- `current.json` 的实际 bootstrap 写入记录见上一条 `her-web release metadata bootstrap`
- 没有重启服务、没有改数据库、没有触发 Dokploy Redeploy

---

### 2026-04-29 local-prod-snapshot 写库锁

> 摘要：给 `local-prod-snapshot` 增加一个很轻的 AI 写库占用锁脚本。多个窗口并行时，先 `status`，需要写库就 `claim`，结束后 `release`；只有确认旧锁失效时才允许 `force-release`。

commit: 未提交（共享 skill / 本地流程记录） | 回滚：删除 `scripts/her-web/local-prod-lock.sh`，恢复 `references/her-web/local-prod-snapshot.md`、`index.md`、`changelog.md`、`/Users/suyuan/.codex/AGENTS.md` 的本次改动

**改动**：
- 新增 `scripts/her-web/local-prod-lock.sh`
  - `status`：查看当前是否有人占用本地生产镜像库
  - `claim <owner> <task> [worktree] [notes]`：声明当前任务成为唯一 writer
  - `release <owner>`：释放自己的锁
  - `force-release`：只有确认旧锁失效时才允许强制清锁
- `local-prod-snapshot.md` 补充锁文件使用方法与并行规则
- `index.md` 增加脚本入口和 changelog 索引
- `/Users/suyuan/.codex/AGENTS.md` 补充：her-web 多任务并行写 `local-prod-snapshot` 前先看锁、再 claim

---

### 2026-04-29 her-web main-only C deploy + local-prod 默认开发

> 摘要：her-web 日常开发/验证默认切到 `local-prod-snapshot`；方案 C 默认只允许从 `main` 部署，并改为只部署 `git archive HEAD`；检测到 schema/migration 变更直接阻断；方案 A 继续只作 GHCR 备份/回滚，不做 TCR，也不加 Docker cache。

commit: 未提交（共享 skill / 全局规则记录） | 回滚：恢复 `scripts/her-web/deploy.sh`、`scripts/her-web/deploy-preflight.sh`、`scripts/her-web/deploy-main-digest.sh`、`references/her-web/standard-deploy.md`、`references/her-web/local-prod-snapshot.md`、`index.md`、`changelog.md`、`/Users/suyuan/.codex/AGENTS.md` 的本次改动

**改动**：
- `deploy.sh` 从直接 rsync 工作区改为 `git archive HEAD` 传输已提交代码；默认只允许 `main` 部署，紧急非 `main` 需要 `ALLOW_NON_MAIN_DEPLOY=1`
- `deploy.sh` 在服务器 build 前清理 `.env.local` / `.env.development` / `.env.test` / `.env.production.local`，并校验单节点 Swarm
- `deploy-preflight.sh` 修正 TypeScript 检查逻辑，增加分支闸门、schema/migration 变更阻断、未跟踪源码文件提示
- `deploy-preflight.sh` 的 schema/migration 基线默认改为 `merge-base(HEAD, origin/main)`，不再只看 `HEAD~1`
- `deploy-main-digest.sh` 文案明确 A 是 GHCR 备份/回滚路径，这轮不做 TCR，不加 cache 假设，并输出当前 service image 方便核对
- `standard-deploy.md` 明确 C 主、A 备、main-only 正常部署；非 `main` 只作紧急止血，且部署后必须补 PR 到 `main`
- `local-prod-snapshot.md` 改成 her-web 默认开发/验证环境，并新增“同一时间只有一个任务能写这套库”的并行规则
- `/Users/suyuan/.codex/AGENTS.md` 补充 her-web 默认环境、worktree 隔离、`suyuan -> *-pr -> main`、main-only 正常部署、紧急例外规则
- 补充 `schema.ts` 的 `skip-worktree` 可能挡住主目录切 `main`；部署验证时优先直接从 `origin/main` 建临时 worktree，不在主目录硬切

**说明**：
- 这轮没有实现 Docker build cache。当前 workflow 会生成 `.env.production` 且 Docker 构建仍读取整个 context，贸然加 `cache-to` 有 secret-bearing layer 风险
- 这轮没有接入 TCR。A 继续保留 GHCR digest 作为备份 / 回滚源

---

### 2026-04-29 her-web 方案 C NEXT_PUBLIC build args（已回滚）

> 摘要：曾尝试让方案 C 在 `.env*` 继续排除 Docker context/cache 的前提下，从服务器 `/home/ubuntu/.her-web-env-production` 只读取公开 `NEXT_PUBLIC_*` 白名单并作为 build args 传入 Docker，避免客户端 bundle 吃到 `ShipAny Next` fallback。此方案未继续采用，相关代码和脚本文档已回滚。

commit: 未提交（本地修复尝试记录） | 状态：已回滚 her-web `Dockerfile`、本 skill `scripts/her-web/deploy.sh`、`references/her-web/standard-deploy.md` 的本次改动；本 changelog 仅保留事故记录

**发生过什么**：
- 一次方案 C 部署后，`/auth-callback` 页面显示 `打开 ShipAny Next 客户端`，不是 `打开 Her 客户端`
- 线上 `/home/ubuntu/.her-web-env-production` 里 `NEXT_PUBLIC_APP_NAME=Her` 正确
- 直接原因是 `.dockerignore` 排除了 `.env*`，服务器构建时 `.env.production` 没进入 Docker build context，Next 客户端 bundle 没拿到 `NEXT_PUBLIC_APP_NAME`，于是使用 `src/config/index.ts` 的 fallback：`ShipAny Next`

**已回滚的尝试**：
- `Dockerfile` 增加 `NEXT_PUBLIC_APP_URL`、`NEXT_PUBLIC_APP_NAME`、`NEXT_PUBLIC_APP_DESCRIPTION`、`NEXT_PUBLIC_APP_LOGO`、`NEXT_PUBLIC_DEFAULT_LOCALE` build args，并在 `pnpm build` 前设为 ENV
- `deploy.sh` 服务器 build 前解析 `/home/ubuntu/.her-web-env-production`，只把上述公开 key 传给 `docker build --build-arg`
- `standard-deploy.md` 增加 Next public env build-arg 说明

**当前提醒**：
- 上述 build-arg 方案已经撤掉，不要按这条记录执行部署
- 之后如果重新修方案 C 的 public env 问题，必须同时验证 `/auth-callback` 文案、logo、`NEXT_PUBLIC_APP_URL`、以及 `.env*` 不进 Docker context/cache

---

### 2026-04-28 her-web Actions cache 安全边界

> 摘要：GitHub Actions 仍会从 `secrets.ENV_PRODUCTION` 写 `.env.production` 供历史流程兼容，但 her-web `.dockerignore` 必须排除 `.env*`，workflow 只允许 `cache-from`，不再 `cache-to` 导出 BuildKit cache，避免 secret-bearing layer 进入 GitHub Actions cache。

commit: 未提交（安全修复文档记录） | 回滚：恢复 her-web `.dockerignore` / `.github/workflows/docker-build.yaml` 以及本 skill `references/her-web/standard-deploy.md`、`SKILL.md`、`index.md`、`changelog.md` 的本次改动

**改动**：
- 方案 A 文档改为“只允许 `cache-from`，不导出 BuildKit cache”
- 记录 `.env*` 必须留在 `.dockerignore`
- 同步更新 `SKILL.md` 和 `index.md` 里的 Actions cache 描述

---

### 2026-04-28 her-web 方案 C 主部署

> 摘要：her-web 生产发布路径改成 C 为主：本地已提交代码 → rsync 到北京服务器 → 服务器 docker build → image id → Swarm start-first → postflight。方案 A（GitHub Actions + GHCR digest）只作备份/回滚，方案 B（旧 preview）只作 legacy/compat；CI 完成不会自动部署或覆盖 C。

commit: 未提交（共享 skill 运维记录） | 回滚：恢复 `scripts/her-web/deploy.sh`、`scripts/her-web/deploy-main-digest.sh`、`scripts/her-web/remote-preview-deploy.sh`、`references/her-web/standard-deploy.md`、`references/her-web/remote-preview-deploy.md`、`SKILL.md`、`index.md`、`changelog.md` 的本次改动

**改动**：
- `deploy.sh` 明确为方案 C，默认拒绝未提交的已跟踪改动，服务器 build 后用 `docker image inspect` 的 image id 执行 `--no-resolve-image --update-order start-first`
- `deploy-main-digest.sh` 明确为方案 A 备份/回滚，service update 改为 `--update-order start-first`；GitHub workflow 后续只保留 `cache-from`，不导出 BuildKit cache，不引入 Tencent/TCR
- `remote-preview-deploy.sh` 和文档标为方案 B legacy/compat，不再推荐为常规生产路径
- `standard-deploy.md` 重写路径分工，明确 A 不会自动接在 C 后执行，CI 只 build + push GHCR，不切生产 service

---

### 2026-04-28 Codex 直接识别 her-ops

> 摘要：确认 Codex 能直接识别 `/Users/suyuan/.claude/skills/her-ops`，不需要 `.codex/skills/her-ops` 软链接。移除 `.codex` 里的 her-ops 软链接和旧 wrapper 备份，避免 UI 出现两个同名 her-ops。

commit: 未提交（本机 skill 注册/文档记录） | 回滚：从废纸篓恢复 `/Users/suyuan/.codex/skills/her-ops` 或旧 wrapper 备份，并恢复 `/Users/suyuan/.codex/AGENTS.md`、`/Users/suyuan/Documents/her-source/CLAUDE.md`、本 skill 的本次记录

**改动**：
- 移除 `/Users/suyuan/.codex/skills/her-ops` 软链接
- 移除 `/Users/suyuan/.codex/skill-backups/her-ops.wrapper-backup-20260428-1`，避免备份里的 `SKILL.md` 被扫成第二个 her-ops
- 全局 AGENTS 和 her-source/CLAUDE 改为明确写 Codex 直接识别 `.claude/skills/her-ops`

---

### 2026-04-28 生产部署默认冻结

> 摘要：线上 her-web / herclub / her-gateway 已经承载真实用户和真实支付链路。把 her-ops 和全局 AGENTS 规则改成默认禁止生产部署、重启、切镜像、改生产库、触发 Dokploy webhook/Redeploy。PR 合并、GitHub build 成功、本地测试通过都不等于上线授权。

commit: 未提交（共享 skill / 全局规则记录） | 回滚：恢复 `SKILL.md`、`index.md`、`changelog.md` 和 `/Users/suyuan/.codex/AGENTS.md` 的本次约束改动

**新规则**：
- 只有用户在当前会话明确说“部署到生产 / 上线生产 / 切线上”，并指定服务和版本/分支，才允许执行生产变更
- 生产变更前必须复述目标服务、版本或 digest、影响域名、回滚方式、预计短暂不可用窗口，并等待再次确认
- her-web / herclub / her-gateway 的部署脚本和 webhook 在没有明确确认时只能作为文档，不执行

---

### 2026-04-28 her-web 本地生产镜像开发入口

> 摘要：把 `local-prod-snapshot` 写入 her-ops：它是本地生产数据镜像，不是代码仓库；日常开发仍在各仓库从 `suyuan` 新建任务分支。本地生产链路用 Postgres + 本地 gateway，her-web `.env.local` 不能默认打线上 gateway；`schema.ts` 如果用了 skip-worktree，做 schema 开发前必须解除。

commit: 未提交（共享 skill 运维记录） | 回滚：删除 `references/her-web/local-prod-snapshot.md`，恢复 `SKILL.md`、`index.md`、`local-dev-chinese-path.md`、`changelog.md` 的本次文档改动

**改动**：
- 新增 `references/her-web/local-prod-snapshot.md`，记录固定目录、端口、her-web env 指向、schema.ts 方言切换、skip-worktree 检查、开发分支边界和支付回调风险
- `SKILL.md` 增加本地生产镜像入口，明确 `local-prod-snapshot` 只放数据/日志/env，不写业务代码
- `local-dev-chinese-path.md` 改成 sqlite/dev 空库流程，避免和生产数据镜像混淆
- `index.md` 增加本地生产镜像场景和 changelog 索引

---

### 2026-04-28 her-web preview env 与 HerClub 联合部署防踩坑

> 摘要：把 04-28 HerClub 紧急上线踩到的两类坑写回 her-ops：her-web preview 部署必须排除并清理本地 `.env.local`，Swarm 更新用 build 后的 image id；HerClub 业务改动要同时验证 her-web API 和 herclub 静态站 bundle。

commit: 未提交（共享 skill 运维记录） | 回滚：恢复 `scripts/her-web/remote-preview-deploy.sh`、`references/her-web/remote-preview-deploy.md`、`references/herclub/deploy.md`、`SKILL.md`、`index.md`、`changelog.md` 的本次文档/脚本改动

**改动**：
- `remote-preview-deploy.sh` 增加 `.env.local` / `.env.development` / `.env.test` / `.env.production` / `data` 排除；rsync 后远端强制删除本地 env 文件，并要求远端已有 `.env.production`
- 同一脚本 build 后读取 `docker image inspect` 的 `sha256:...`，`docker service update` 改用 image id + `--no-resolve-image --update-order start-first`
- `remote-preview-deploy.md` 补齐当前临时目录 `/tmp/her-web-preview-herclub-entitlement-20260428-1`、本地 env 泄漏、`.env.production` build 日志、tag 解析、Dokploy Redeploy 回滚五类风险
- `herclub/deploy.md` 补联合改动检查：先测 her-web API，再确认 club 静态 bundle，最后用邮箱恢复/会员卡接口做命令验证；记录 herclub 更新时短暂 502 / `Preparing` 不一定是坏了
- `SKILL.md` / `index.md` 同步更新入口，避免只看概要时漏掉新规则

---

### 2026-04-28 herclub 首次部署上线（club.hersoul.cn）

> 摘要：herclub（Vite+React 静态 SPA）首次部署到 192.144.187.174。Nginx Docker 容器 + Swarm service + Traefik 路由 + Let's Encrypt SSL。部署方式：SSH 直连（本地 build → rsync → 服务器 docker build → Swarm service），不走 CI/CD。一键脚本 `scripts/herclub/deploy.sh`。

回滚：`/usr/bin/ssh -n ubuntu@192.144.187.174 'sudo docker service scale herclub=0'`（下线）；恢复 `scale herclub=1`

**部署细节**：
- DNS：腾讯云 DNSPod A 记录 `club` → `192.144.187.174`
- Traefik 配置：`/etc/dokploy/traefik/dynamic/herclub.yml`
- Swarm service：`herclub`，网络 `dokploy-network`，镜像 `herclub:latest`（服务器本地 build）
- SSL 首次签发因 DNS 未传播收到 NXDOMAIN，移除再恢复 Traefik 动态配置触发重试后签发成功
- SKILL.md 新增 herclub 段落（仓库地图、服务拓扑、域名、最小路径、脚本表）
- 新增 `references/herclub/deploy.md` 部署文档 + `scripts/herclub/deploy.sh` 一键脚本

**涉及文档**：SKILL.md, index.md, references/herclub/deploy.md, scripts/herclub/deploy.sh

---

### 2026-04-27 新增 mimo-v2.5 模型 + 清理多余 Claude 定价条目

> 摘要：线上新增 mimo-v2.5 模型（定价、渠道、模型管理），清理了 19 条不在渠道里的 Claude 历史模型定价条目，最终线上只保留 9 个实际使用的模型。

回滚：admin API PUT `/api/option/` 删除 mimo-v2.5 条目；渠道 id=5 models 字段去掉 `mimo-v2.5`；模型管理删除 id=10

**新增 mimo-v2.5**：
- 定价：输入 ¥2.80/MTok（ratio=1.4）、输出 ¥14.00/MTok（completion_ratio=5.0）、缓存读 ¥0.56/MTok（cache_ratio=0.2）、缓存写免费（create_cache_ratio=0）
- 渠道：加入小米 MiMo Coding Plan #1（id=5），models 现为 `mimo-v2-pro,mimo-v2.5-pro,mimo-v2.5`
- 模型管理：model_meta id=10，vendor_id=3（小米），name_rule=0（精确匹配）
- 测试：通过 `/v1/messages`（Claude 协议端点）调通，返回正常

**清理多余条目**：
- 从 ModelRatio / CompletionRatio / CacheRatio / CreateCacheRatio 中删除 19 条不在任何活跃渠道里的 Claude 历史模型（haiku 系列、sonnet 3.x 系列、旧 opus 系列、opus-4-6 的 max/high/medium/low 变体）
- 清理后线上定价表只保留 10 个模型：claude-opus-4-7/4-6/4-5、claude-sonnet-4-6、glm-5/5-turbo/5.1、mimo-v2-pro/v2.5-pro/v2.5

**注意**：MiMo 全系列只支持 Claude 协议（`/v1/messages`），不走 OpenAI 兼容接口（`/v1/chat/completions`），这是 adaptor 设计决定的，不是 bug。

---

### 2026-04-27 her-web 标准部署上线（issue-13/14/15 + schema 模板同步修复）

> 摘要：合并 suyuan→main 3 个已完成任务（gateway quota 修复、模型列表、邀请码 HerClub pipeline）。首次 CI build 失败——schema.ts 被切成 sqlite 方言推到了 CI，报 `uniqueIndex` 找不到。修复：同步三份 schema 模板（postgres/sqlite/schema.ts），补 herClub 列、tier、uniqueIndex、usagePolicyId、三张新表。二次 push CI 通过。digest 直更 Swarm 部署成功。生产 Postgres 补执行 `ALTER TABLE invite_code ADD COLUMN tier` + `CREATE UNIQUE INDEX uniq_user_invite_user_code`。

回滚：`docker service update --image ghcr.io/her-os/her-web@sha256:0b0f227128d177c44f70ef52318f15da7fe8788f1b83b6f0399660f3212ea850 --force her-herweb-a8y5ka`

**教训**：schema.ts 没被 gitignore，本地切成 sqlite 后推 CI 会炸。三份模板（postgres/sqlite/schema.ts）必须同步——以后新增表/列时一并更新所有模板。

---

### 2026-04-27 her-web 生产数据库紧急迁移（HerClub + 产品线 schema）

> 摘要：艾逗笔 push 含 schema 变更的代码到 main，Actions 自动构建并部署了新镜像，但未执行数据库迁移。生产 PostgreSQL 缺列导致 better-auth 全部查询报 `column "her_club_tier" does not exist`，所有用户登录/注册全挂。手动执行 ALTER TABLE + CREATE TABLE 修复。

回滚：DROP 新增的 3 张表 + ALTER TABLE DROP COLUMN 去掉新列（但目前新代码依赖这些列，回滚需同时回滚镜像到 `sha256:0b0f227128d1`）

**变更内容**：
- `user` 表：新增 5 列（`her_club_tier`, `her_club_source`, `her_club_note`, `her_club_granted_by`, `her_club_granted_at`）+ 1 索引
- `subscription` 表：新增 5 列（`product_line`, `plan_id`, `model_regions`, `usage_policy_id`, `source`）+ 1 复合索引
- 新建表：`her_club_audit`（HerClub 操作审计）、`usage_record`（用量记录）、`manual_recharge`（手动充值台账）
- 全部使用 `IF NOT EXISTS` 保证幂等

**根因**：代码部署流水线（push → Actions build → 镜像上线）与数据库迁移是解耦的，后者需手动执行。艾逗笔漏了迁移步骤。

**教训**：schema 变更的 PR 应在 commit message 或 PR body 标注"需执行迁移"，部署后验证 auth 端点。

---

### 2026-04-26 Claude 模型定价 ×7 + 模型管理补齐 + SSH 公钥

> 摘要：线上 her-gateway 所有 Claude 模型输入倍率乘 7（¥1=$1 中转站定价），补齐模型管理表 9 条记录和 3 个供应商，新增 Opus 4.7 / Sonnet 4.6 两个新模型，添加 Tiny 的 SSH 公钥到服务器。

回滚：通过 admin API PUT `/api/option/` 把 ModelRatio 恢复原值；模型管理条目可逐条删除

**定价更新（admin API 热更新，立刻生效）**：
- ModelRatio：23 个 Claude 模型全部 ×7（Haiku 0.875、Sonnet 10.5、Opus 4.5/4.6/4.7 = 17.5、旧 Opus 52.5）
- CompletionRatio：同步补齐 23 条（输出/输入比 = 5，不变）
- CacheRatio：同步补齐 23 条（读缓存 0.1，不变）
- CreateCacheRatio：同步补齐 23 条（写缓存 1.25，不变）
- 新增模型：`claude-opus-4-7`（$5/MTok × 7 = 17.5）、`claude-sonnet-4-6`（$3/MTok × 7 = 10.5）
- 非 Claude 模型（glm-5 系列、mimo 系列）保持原值不变
- 源码 `setting/ratio_setting/model_ratio.go` 和 `cache_ratio.go` 同步修改（下次部署生效）

**模型管理（model_meta 表，纯展示目录不影响路由）**：
- 新建供应商：Anthropic（id=2）、小米（id=3），智谱已有（id=1）
- 新建 9 条模型记录，全部 name_rule=0（精确匹配）、status=1（启用）：
  - Anthropic：claude-opus-4-7, claude-opus-4-6, claude-opus-4-5, claude-sonnet-4-6
  - 智谱：glm-5, glm-5-turbo, glm-5.1
  - 小米：mimo-v2-pro, mimo-v2.5-pro
- missing 接口验证通过（0 条缺失）

**SSH 公钥**：
- 添加 Tiny 的 ed25519 公钥到 `ubuntu@192.144.187.174:~/.ssh/authorized_keys`

**验证**：
- admin API GET `/api/option/` 确认 23 条 Claude ModelRatio 值正确
- missing 接口返回 `null`（0 条缺失）
- 源码 `go build` 通过

---

### 2026-04-25 her-web digest 直更部署 + salon 后端切换

> 摘要：不换镜像仓库的前提下，把 her-web 标准部署从“触发 Dokploy webhook”改为“读取/指定 GHCR digest → 服务器显式 pull → Swarm service update 指定 digest”；同时给 salon 增加 `local/cloud/prod/status` 后端切换脚本，避免 dev 模式云端联调时 `her://` 打开已安装客户端。

commit: 未提交（共享 skill 运维记录 + salon 工作分支脚本） | 回滚：her-web 部署可继续用 Dokploy UI/webhook；salon 可手动编辑 `.env.local` 的 `VITE_HER_*` 三行

**改动**：
- 新增 her-ops 脚本 `scripts/her-web/deploy-main-digest.sh`
  - 不传 digest 时读取最新成功的 `main` / `docker-build.yaml` Actions 日志里的 `containerimage.digest`
  - 支持 `--dry-run` 和 `--verify-only`
  - 标准执行：server pull digest → `docker service update --image ghcr.io/her-os/her-web@sha256:<digest> --force --update-order stop-first her-herweb-a8y5ka` → 查 task/container → curl `https://hersoul.cn`
- `references/her-web/standard-deploy.md` 改为 digest 直更 Swarm 为首选，Dokploy webhook 降级为备用
- salon 新增 `/Users/suyuan/Documents/her-source/salon/scripts/switch-her-backend.sh`
  - `local`：`http://localhost:3000` + `127.0.0.1:17693` callback
  - `cloud`：`https://hersoul.cn` + `127.0.0.1:17693` callback
  - `prod`：删除 `VITE_HER_*` 覆盖，回到 `https://hersoul.cn` + `her://...` 默认
  - `status`：查看当前 `.env.local`
- 新增 `references/salon/backend-switching.md`，`SKILL.md` / `index.md` 同步加入 salon 场景

**验证**：
- salon 切换脚本用临时 `ENV_FILE` 验证 `local → cloud → prod → status`，确认保留其它 env 键
- salon `pnpm vitest run src/lib/__tests__/her-backend.test.ts` 通过
- her-web digest 部署脚本 `bash -n`、`--dry-run`、自动读 digest 验证通过；随后用当前线上 digest 完整执行了一次真实部署，`docker service update` 收敛，新 task 跑目标 digest，`https://hersoul.cn` HTTP 200，30 秒后复查仍正常

---

### 2026-04-25 her-web 认证/首页/余额修复上线

> 摘要：PR #11 合入 `main`，上线营销登录修复、首页详情卡片/视频更新、长邮箱导致的余额同步失败修复。标准 her-web 镜像构建成功后，Dokploy webhook 返回 200 但 Swarm task 没切换；按 `standard-deploy.md` 的 3A 路径，服务器显式 pull 新 digest 后用 `docker service update --image <digest> --force --update-order stop-first` 切换成功。

commit: `d243075`（main）/ `533ba24`（suyuan 产品修复） | PR: https://github.com/her-os/Her-Web/pull/11 | 镜像 digest: `sha256:0b0f227128d177c44f70ef52318f15da7fe8788f1b83b6f0399660f3212ea850`

**改动**：
- 登录/注册营销页修复随 PR #11 一起上线
- 首页详情卡片和 `public/her/her-video-web.mp4` 更新随 PR #11 一起上线
- her-web gateway provisioning 修复：`display_name` 截断到 20 字符，长邮箱 username 截断到 50 字符并保留稳定 userId 后缀
- `GET /api/user/quota` 缺少 `user_gateway` 时会尝试 lazy provisioning；客户端不再看到 raw gateway validation error
- her-gateway 不提交、不部署；这次余额问题根因在 her-web

**验证**：
- PR build 成功：GitHub Actions run `24927451636`
- main build 成功：GitHub Actions run `24927508096`
- 生产 Swarm task 已运行新 digest：`ghcr.io/her-os/her-web@sha256:0b0f227128d177c44f70ef52318f15da7fe8788f1b83b6f0399660f3212ea850`
- `https://hersoul.cn` 返回 HTTP 200

---

### 2026-04-25 her-web 飞书内测表单恢复

> 摘要：线上 `/api/beta/apply` 失败不是飞书挂了，而是生产缺 `FEISHU_*` 环境变量。处理顺序：从当前 service env 合成新的 `ENV_PRODUCTION` 并补上 `FEISHU_*`，手动重跑 her-web `main` 镜像构建；Dokploy webhook 没把 Swarm 换到新 task，最后按单节点 Swarm 的本地镜像路径，用 `origin/main` + 修正后的 `.env.production` 在服务器构建 `her-web:preview-feishu`，`docker service update --no-resolve-image --force --update-order stop-first` 切换成功；新容器 `.env.production` 已含 `FEISHU_*`，飞书 tenant token 握手返回 `code=0`.

commit: 未提交（共享 skill 运维记录） | 回滚：Dokploy 或 `docker service update --image ghcr.io/her-os/her-web:main --force her-herweb-a8y5ka` 切回标准镜像；若需撤销本地镜像可删 `her-web:preview-feishu`

**改动**：
- GitHub `ENV_PRODUCTION` secret 已补齐 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` / `FEISHU_BASE_TOKEN` / `FEISHU_TABLE_ID`
- GitHub Actions `docker-build.yaml` 手动重跑成功，产出新的 `main` 镜像 digest `sha256:5df640e8cf3db23bb2a4b531d8e3f6ff1e0cbaa3d230a46bbc756dbccea44faf`
- Dokploy webhook 返回 200，但未真正把 Swarm service 切到新 task；直接 `docker service update --force` 也先卡在 `Preparing`
- 最终通过服务器本地构建 `her-web:preview-feishu` 先救活 service，再切回标准 ghcr digest `ghcr.io/her-os/her-web@sha256:5df640e8cf3db23bb2a4b531d8e3f6ff1e0cbaa3d230a46bbc756dbccea44faf`
- 验证：新容器 `/app/.env.production` 含全部 `FEISHU_*` 键；容器内直打飞书 token 接口返回 `{\"ok\":true,\"code\":0,\"msg\":\"ok\"}`

---

### 2026-04-25 her-web 本地 sqlite/dev 文档改版 + 部署数据库边界补齐

> 摘要：把 her-web 当前 ASCII 路径下的本地 sqlite/dev 闭环升为主路径，旧中文路径 panic 降为历史兼容说明；同步把标准部署和 preview 部署都补上“不会自动动生产 Postgres”的边界，并把 `local-mirror-sync.sh` 默认源路径改成 `/Users/suyuan/Documents/her-source/her-web`。

commit: 未提交（共享 skill 本地文档 / 脚本同步） | 回滚：手动恢复 `references/her-web/local-dev-chinese-path.md` / `standard-deploy.md` / `remote-preview-deploy.md` / `index.md` / `SKILL.md` 的旧描述，并把 `scripts/her-web/local-mirror-sync.sh` 默认源路径改回旧中文路径

**改动**：
- `references/her-web/local-dev-chinese-path.md` 改成“当前 her-web 本地 sqlite/dev 标准闭环 + 旧中文路径兼容”的文档结构，新增当前推荐流程、生产边界、当前状态说明
- `references/her-web/standard-deploy.md` 明写 Dokploy redeploy 只换容器，不会自动删库或跑迁移；补充 `.dockerignore` 已排除本地 sqlite
- `references/her-web/remote-preview-deploy.md` 明写 preview deploy 同样不自动跑迁移、不打包本地 `data/local.db`
- `scripts/her-web/local-mirror-sync.sh` 默认源路径改成 `/Users/suyuan/Documents/her-source/her-web`
- `SKILL.md` / `index.md` 同步把 her-web 本地开发的主叙事从“中文路径绕行”改成“当前 sqlite/dev 闭环，中文路径是历史兼容”

---

### 2026-04-23 her-web 本地 auth + sqlite 闭环补齐

> 摘要：`local-mirror-start.sh` 改成先写非默认 `AUTH_SECRET` / `BETTER_AUTH_SECRET`，再跑 `pnpm db:setup` + `pnpm db:push`；本地文档补齐 sqlite 空库、邀请码、测试账号、`Invalid origin` 的排障路径。

commit: 未提交（共享 skill 本地文档 / 脚本同步） | 回滚：手动把 `scripts/her-web/local-mirror-start.sh` 恢复成 `pnpm install + db:push` 流程，并删去 `references/her-web/local-dev-chinese-path.md` / `SKILL.md` / `index.md` 中新增的 auth/sqlite 闭环说明

**改动**：
- `references/her-web/local-dev-chinese-path.md` 从“只讲中文路径 panic”扩成“本地开发与数据库初始化”文档，补齐 `AUTH_SECRET`、`db:setup`、sqlite 空库、邀请码、测试登录账号、`Invalid origin`、`pnpm build && pnpm start` 验证
- `scripts/her-web/local-mirror-start.sh` 首次启动默认生成非默认 secret，并执行 `pnpm db:setup` + `pnpm db:push`
- `SKILL.md` / `index.md` 同步更新 trigger、摘要、脚本说明和场景入口，避免正文更新后入口仍指向旧认知

---

### 2026-04-23 新增 CodingRouter Anthropic 渠道 + claude-opus-4-5 映射修复

**渠道**：id=6，base_url=https://api.codingrouter.com，模型 claude-opus-4-7 / claude-opus-4-6 / claude-opus-4-5 / claude-sonnet-4-6，status=1，auto_ban=0。

**定价**（input $5 / output $25 / cache 命中 $0.5 / cache 创建 $6.25，单位 $/1M）：
- ModelRatio=2.5, CompletionRatio=5.0, CacheRatio=0.1, CreateCacheRatio=1.25（四个模型相同）

**模型映射**：上游不认 `claude-opus-4-5` 短名，加 model_mapping `{"claude-opus-4-5": "claude-opus-4-5-20251101"}` 解决。

**测试**：四个模型均通（claude-opus-4-7 慢，正常）。测试 token id=12 已删除。

**回滚**：Admin API `PUT /api/channel/` 把 id=6 status 改 2（禁用）；或 `DELETE /api/channel/6`。

---

### 2026-04-21 admin 代户接口上线 + 部署文档全量修正

**部署**：PR #2（her-web-admin-proxy-apis）合入 suyuan → merge main → CI 推 ghcr → 手动 docker pull → Dokploy redeploy → 验证 HTTP 200。生产版本 `main-20260421-884940e`。

**新增**：9 个 admin 代户操作接口（`/api/user/:id/quota` 等），幂等充值（Idempotency-Key 4-tuple），token TOCTOU 修复。

**skill 修正**（skill-improve 驱动）：
- SKILL.md 部署段从"两步走"改成"三步走"（加 docker pull）
- SKILL.md 服务拓扑加 port 3000 碰撞警告
- topology.md 修正"自动部署"过时描述
- dokploy-deploy.md 修正 `docker compose pull` 矛盾描述 + 时间线表 + 踩坑记录
- admin-api.md 补全 9 个新端点
- local-docker-dev.md / dokploy-deploy.md 路径从中文改为 ASCII（`~/Documents/her-source/`）
- channel-provisioning.md 修正内部引用路径

**回滚**：代码 `git revert eefe420`；skill 文件 `git -C ~/.claude/skills/her-ops checkout HEAD~1 -- .`

---

### 2026-04-18 部署从"push main 自动"改回"手动触发"（两仓库对齐）

**关联**：反转 04-17 的 `a34c9cb0`（her-gateway 给 CI 加 curl Dokploy webhook）+ 澄清 her-web 从来没有过自动部署。

**背景**：
- her-gateway `docker-image-main.yml` 的最后一步 curl Dokploy webhook，每次 push main → 触发全量 redeploy → **约 10 分钟停机**。高频上线时非常痛。
- her-web 今天合两个 PR（landing + 飞书 relay）后发现 hersoul.cn 没变，排查才发现：her-web CI **从来没有** curl webhook 这一步，根本没有自动部署。过去 `standard-deploy.md` 里写的 "Dokploy watch ghcr → auto-pull" 是错的——Dokploy 对 `sourceType=docker` 的应用**不 polling ghcr**，`autoDeploy=true` flag 只在外部 webhook 进来时生效。
- 同时发现 her-herweb-a8y5ka service 被历史 preview 部署切到了 `her-web:preview` 本地 image，Dokploy redeploy 会把它切回 ghcr.io/her-os/her-web:main。

**改动**：
- **her-gateway**：删 `.github/workflows/docker-image-main.yml` 的 "Trigger Dokploy deploy" step（commit `343e215b` on suyuan → merged to main via PR #1 as `a716716`）。最后一次 push main 会用 old CI 最后一口气触发一次 Dokploy 部署（停机 10 分钟），之后永不自动。
- **her-web**：无代码改动（CI 本来就没 curl webhook）。但 `references/her-web/standard-deploy.md` 整份重写——修正"Dokploy auto-pull" 错误描述，改成明确的"push 只 build ghcr，deploy 必须手动触发"，列三种触发方式（Dokploy UI / curl webhook `POST /api/deploy/<token>` / ssh docker service update）、记录 refreshToken `wmfBMZ5AZhVaN7WnHsUm6` 和关键配置。

**手动触发（二选一）**：
```bash
# her-web（Application 类型，路径不带 compose）
curl -sS -X POST "https://dok.tokenic.cn/api/deploy/wmfBMZ5AZhVaN7WnHsUm6"

# her-gateway（Compose 类型，路径带 compose）
curl -sS -X POST "https://dok.tokenic.cn/api/deploy/compose/qpfrEdx5Cf-fTe-ZTb0I-"
```

**回滚**：
```bash
# 如果决定让 her-gateway 恢复自动：revert 343e215b
git -C <her-gateway> revert 343e215b && git push origin main
# her-ops skill 文档回退
git -C ~/.claude/skills/her-ops checkout HEAD -- references/her-web/standard-deploy.md changelog.md
```

**教训**：
1. 以为 "Dokploy watch ghcr auto-pull" 是自动的，是典型的 vendor 文档字面读出来的误解。Dokploy 对 docker-image 应用没有 polling 机制——要么配 CI webhook、要么手动。
2. 两仓库部署流程要对齐，不然一个认知（"her-gateway 自动"）外溢到另一个（"her-web 也自动"）会排错排很久。
3. Stack PR 合并时**不要** `--delete-branch`——下游 PR 的 base 分支被删 → GitHub 自动 close 下游 PR → 不能 reopen + 改 base，只能新开 PR。

→ 详见 `references/her-web/standard-deploy.md`（已整份重写）

---

### 2026-04-18 her-web 日常 dev mirror 同步补丁 + trigger 词扩宽

**背景**：her-web 源仓库改完代码后，如果 dev server 跑在 `/tmp/her-web-local` 镜像里，HMR **只认镜像文件**——源仓库的改动不会自动过去。今天实装 landing snap + Entry 回滚时踩的：AI 改了源码 → 浏览器没反应 → 以为修复没生效，实际是镜像没同步。另一条相关坑：源仓库 `.env.local` 新加的 `FEISHU_*` 四个 key 也没到镜像，`process.env.FOO === undefined` 让飞书调用直接挂。

**改动**：
- 新增 `scripts/her-web/local-mirror-sync.sh` — 增量 rsync（不 install / 不 db / 不 dev），幂等可反复跑；末尾自检 `:3000` cwd 是不是镜像 + diff `.env.local` 提示源 vs 镜像 key 差异
- `references/her-web/local-dev-chinese-path.md` 新增「日常开发循环」段 — 明写"HMR 看镜像不看源仓库"的认知；列出每次改完源码跑的同步命令；`.env` 新 key 要 kill 老 dev 重启（不热更）；什么情况要整重建而不是 sync（package.json / schema / env 新 key / Next 版本升级）
- `SKILL.md` trigger 词扩宽：加 `本地 dev mirror 同步 / HMR 没反应 / 改了代码没生效 / 改了 .env.local 不热更 / /tmp/her-web-local 镜像`
- `SKILL.md` 路由表新增一行映射日常同步场景 → `local-dev-chinese-path.md「日常开发循环」段`

**回滚**：
```bash
git -C ~/.claude/skills/her-ops diff HEAD  # 确认改动范围
git -C ~/.claude/skills/her-ops checkout HEAD -- SKILL.md references/her-web/local-dev-chinese-path.md changelog.md
rm ~/.claude/skills/her-ops/scripts/her-web/local-mirror-sync.sh
```
→ 详见 `references/her-web/local-dev-chinese-path.md`「日常开发循环」段 + `scripts/her-web/local-mirror-sync.sh`

---

### 2026-04-18 her-gateway 部署自动化最后闭环 + 端到端验证通过

commit: `a34c9cb0`（Actions 最后一步 curl Dokploy webhook）| 回滚：`git revert a34c9cb0`，部署会退回"Actions build 完成但不自动触发 Dokploy"的半手动状态
**背景**：04-17 原始改造（`a9f2b469`、`832f259e`）只做到了 Actions build + push ghcr。Dokploy 端因为 sourceType=raw 没源，GitHub push webhook 不能直达 Dokploy。因此 build 完后还得手动去 Dokploy 面板按 redeploy。这次补上 Actions 最后一步 curl webhook，才真正闭环。
**关键动作**：
1. GitHub Secret 配 `DOKPLOY_WEBHOOK_TOKEN`（值同 `qpfrEdx5Cf-fTe-ZTb0I-`）
2. workflow 最后一步加 `Trigger Dokploy deploy`（soft fail：webhook 挂了只 warning 不让 Actions 变红，保住镜像 published 的状态）
3. merge suyuan → main 做了一次完整端到端验证：push → Actions 5m27s 跑完 → webhook HTTP 200 → Dokploy deployment `6tol7Vg4GdblBV6rroPyJ` running → docker compose pull ghcr（腾讯云 pull 镜像 5-10 分钟）→ 容器切换 → API 200
**修 skill**：
- `channel-provisioning.md` 标注业务决策 `auto_ban=0`（新建渠道统一不开，避免 testChannel 误判触发定时巡检禁用）
- `SKILL.md` 改 compose 描述（raw 模式只读 DB，不读仓库），部署段补"合完切回 suyuan 分支"提醒
- `dokploy-deploy.md` 加实测时间线表 + Dokploy pull 假死识别
→ 详见 dokploy-deploy.md（实测时间线段）、SKILL.md（her-gateway 细节段）

---

### 2026-04-18 skill 升级：her-gateway-ops → her-ops（Her 产品双仓库运维）

**目录名改动**：`her-gateway-ops/` → `her-ops/`（老名字只反映 gateway 单仓库语义，改名后覆盖 her-gateway + her-web 双仓库运维）。备份目录同步改名为 `her-ops.bak-20260418/`（SKILL.md 已改名为 SKILL.md.disabled 避免被 skill loader 识别）。
- **扩容 SKILL.md**：单仓库（her-gateway） → 双仓库（her-gateway + her-web）
  - description 加 her-web / hersoul.cn / Turbopack / 紧急 preview 部署 等 trigger 词
  - 新增「仓库路由表」「仓库地图」「her-web 特有坑」章节
  - 原「服务拓扑 / 域名 / 最小路径」拆为 her-gateway 和 her-web 两段
  - 黑灯新增：her-web 紧急临时部署必须用户明确指令才执行
- **新增 references/her-web/** 3 个 playbook：
  - `local-dev-chinese-path.md` — Turbopack 16.2.3 中文路径 panic 根因 + rsync 到 `/tmp/her-web-local/` 方案
  - `remote-preview-deploy.md` — 紧急绕 CI 部署 playbook（rsync + server build + swarm service update + 单节点前提 + 生命周期 + 收敛回标准状态）
  - `standard-deploy.md` — 标准 CI/CD 流程说明（push main → Actions → ghcr → Dokploy auto-pull）
- **新增 scripts/her-web/** 2 个脚本（chmod +x）：
  - `local-mirror-start.sh` — 一键 rsync + .env.local 生成 + pnpm install + db:push（参数化 SRC/DST）
  - `remote-preview-deploy.sh` — 一键 rsync + server build + service update（参数化 SERVER/SERVICE/TAG，带单节点 Swarm 前置校验）
- **原 references/\*.md（7 个）** 全部迁到 `references/gateway/`：admin-api / channel-provisioning / dokploy-deploy / domains-and-ssl / local-docker-dev / topology / zhipu-coding-plan
- **原 scripts/\*.sh（3 个）** 全部迁到 `scripts/gateway/`：backup-configs / health-check / stress-test
- **index.md 升级** 为双仓库索引：按仓库分两块表（保留原摘要/最后更新列）+ 按场景快速查找短表
- **回滚**：`rm -rf ~/.claude/skills/her-ops && mv ~/.claude/skills/her-ops.bak-20260418/SKILL.md.disabled ~/.claude/skills/her-ops.bak-20260418/SKILL.md && mv ~/.claude/skills/her-ops.bak-20260418 ~/.claude/skills/her-gateway-ops`
→ 详见 references/her-web/ 下 3 个新文档

---

### 2026-04-17/18 部署链路改造：tar.gz 应急 → GitHub Actions + GHCR + Dokploy raw compose

commit: `a9f2b469`（workflow + compose image 模式）, `832f259e`（OCI source label）| 回滚：两个 commit 都 revert，把 Dokploy compose sourceType 改回 github（本地 `docker-compose.yml` 先改回 `build:` 模式、再 scp 覆盖 code 目录）。
**背景**：04-16 开始 Dokploy 走 git clone 国内不稳，踩了 3 次后抛弃。艾逗笔给 her-web 用的方案（Docker Image 拉 ghcr）启发，全面切换到该路径。
**关键动作**：
1. 仓库 `suyuan2022/her-gateway` transfer 到 `her-os/her-gateway`（private），本地 remote 切到 her-os
2. 新建 `.github/workflows/docker-image-main.yml`：push to main 触发 build + push `ghcr.io/her-os/her-gateway:main`+`:sha-<短哈希>`，+ curl Dokploy webhook 自动触发部署（GitHub Secret `DOKPLOY_WEBHOOK_TOKEN`）
3. 改 `docker-compose.yml`：`build: context: .` → `image: ghcr.io/her-os/her-gateway:main` + `pull_policy: always`
4. Dokploy PG `UPDATE compose SET sourceType='raw', composeFile='<新 YAML>'`，彻底绕开 GitHub clone
5. her-os 组织 Packages permissions 勾允许 Internal；her-gateway package 可见性改 Internal 解决 PAT 拉不到
6. 新建 `suyuan` 分支做开发、main 做生产；PR 流走合并
**坑**：sourceType=raw 后改 docker-compose.yml 要**双写**——仓库和 Dokploy DB 的 `composeFile` 字段。光改仓库不同步 DB 没效果。
**验证**：容器切到 `ghcr.io/her-os/her-gateway:main` healthy，API 200，用户数据完全保留。
→ 详见 dokploy-deploy.md（全文重写）、topology.md（Dokploy/GitHub 段重写）

---

### 2026-04-17 修复 testChannel 对 Anthropic-only 渠道的假失败 + 云端部署

commit: f8fd5544 | 回滚: git revert f8fd5544 + 重部署 tar.gz（旧代码备份在 `/etc/dokploy/compose/her-newapi-e91gqn/code.bak.1776425405`）
发现后台点 MiMo「测试渠道」按钮假失败，根因 testChannel 默认走 OpenAI 格式但 MiMo 只支持 Anthropic；引入 `common.IsAnthropicOnlyChannel` 白名单 + `RelayModeClaude` + testChannel 三处分支重构。先通过 admin API 把线上 MiMo 渠道（id=5）auto_ban 从 1→0 止血，再修代码 + Codex 双模型复审通过 + 部署 + admin API 验证测试成功。
→ 详见 channel-provisioning.md（白名单集成点 + testChannel 假失败识别）、dokploy-deploy.md（build log 假死识别）、admin-api.md（鉴权方式更正）

---

### 2026-04-16 添加 Tiny 的 SSH 公钥到 gateway 服务器

追加 `ssh-ed25519 ...Cm8pkAq tiny@TinyZhdeMacBook-Pro.local` 到 `ubuntu@192.144.187.174:~/.ssh/authorized_keys`，现 2 个 key，权限 600。
回滚：`ssh ubuntu@192.144.187.174 "sed -i '/tiny@TinyZhdeMacBook-Pro.local/d' ~/.ssh/authorized_keys"`

---

### 2026-04-16 mimo 渠道接入 + 本地端到端验证链路

新增 her-gateway channel type 58（小米 MiMo Coding Plan），走 Anthropic 协议，复用智谱的 system_prompt_rewrite + header_override 身份伪装；本地 docker 起起来后建渠道配定价（¥7/1.4/21 → ratio 3.5/0.2/3/0）、真实 key 发流式请求、验证 logs 表计费准确到 token。
→ 详见 channel-provisioning.md, local-docker-dev.md

---

### 2026-04-15 分支分离：main 做生产、her 做开发

Dokploy 部署分支从 her 改为 main，Trellis create_pr 默认分支改为 her
→ 详见 topology.md, dokploy-deploy.md

---

### 2026-04-15 GLM 渠道亲和性 + healthcheck 修复 + Traefik labels 硬编码

commit: 23934b27, 9761fc6a, 4fe7d82e | 回滚: git revert 23934b27
→ 详见 zhipu-coding-plan.md, dokploy-deploy.md

---

### 2026-04-15 验证智谱额度监控功能

两个账号 JWT 均正常返回额度数据，后台任务运行中
→ 详见 zhipu-coding-plan.md

---

### 2026-04-15 修复模型定价显示

通过 PostgreSQL 直接更新 ModelRatio 等 4 个 options，改为 1USD=1CNY 直显模式
→ 详见 admin-api.md

---

### 2026-04-15 创建运维 skill

初始版本，后经重构为索引+经验文档结构

---

### 2026-04-14 Coding Plan 功能实现 + 云端部署

commit: 7a42d9ed ~ 7f2a4cae（7 个） | 回滚: git revert 对应 commit
→ 详见 zhipu-coding-plan.md, dokploy-deploy.md

---

### 2026-04-14 SSL 证书问题发现

api.roome.cn 自签名，根因 roome.cn 无 ICP 备案
→ 详见 domains-and-ssl.md

---

### 2026-05-03 Roome agent-ops gateway ingest

roome 测试栈新增 web/gateway 共享的 agent-ops ingest 密钥文件 `/home/ubuntu/roome-test/agent-ops.env`，`roome-test.sh deploy-web` 会把 `AGENT_OPS_INGEST_SECRET` 注入 her-web，新增 `deploy-gateway <her-gateway-worktree>` 会从指定 gateway worktree 构建本地镜像并注入：

- `HER_WEB_AGENT_OPS_INGEST_URL=http://her-web-roome-test:3000/api/agent-ops/ingest`
- `HER_WEB_AGENT_OPS_INGEST_SECRET`
- `HER_WEB_AGENT_OPS_MAX_BODY_BYTES=262144`

已部署验证：

- web: `her-web:roome-test-20260503024439`
- gateway: `her-gateway:roome-test-20260503024840`
- IP gateway 请求 `202605021902015413374158268d9d6cALnfPMj` 已从 `/roome-gateway/v1/chat/completions` 导入 her-web `agent_conversation_log` 和 `agent_behavior_event`。

注意：gateway ingest 只影响 roome 测试服务；未配置 `HER_WEB_AGENT_OPS_*` 的环境不会导出日志。真实 Claude Code 本机工具/文件/shell/browser/skill 行为仍需要客户端或 launcher telemetry emitter。

---

### 2026-05-03 Roome agent-ops client event ingest

roome 测试栈验证 her-web 新增 `POST /api/agent-ops/events`：客户端用用户自己的 her-gateway API key 上报 Claude Code 行为事件，服务端按 `client_event_id` 做幂等，避免重试重复记账。

`roome-test.sh deploy-web` 修复为同时 patch：

- base roome web DB：`her-web-roome-db`
- 当前 active clone DB：读取 `/home/ubuntu/roome-test/web-db-active.env` 的 `WEB_DB_URL`

已部署验证：

- web: `her-web:roome-test-20260503034233`
- API proof: `roome-client-telemetry-20260503033138` 首次写入 2 条事件，重复请求返回 0 条新增、2 条重复
- Browser E2E: `roome-client-ui-visible-20260503034652` 在 `http://192.144.187.174/zh/admin/agent-ops` 的行为轨迹页显示 `命令执行` 和 `工具调用`，详情能看到 payload

注意：这只是服务端接收契约；真正的 Claude Code 本机工具/文件/shell/browser/skill 采集器仍需在客户端/launcher/salon 后续实现。

---

### 2026-05-03 Roome agent-ops post-check hardening

post-check 后 roome 测试栈更新到：

- web: `her-web:roome-test-20260503041057`
- gateway: `her-gateway:roome-test-20260503024840`

`roome-test.sh deploy-web` 的 schema patch 已补齐：

- `idx_user_gateway_api_key`
- `uniq_agent_conv_request_id`
- `uniq_agent_event_request_type`
- `uniq_agent_event_user_client_event`

已验证：

- active clone DB 确认 4 个索引存在
- API proof: `roome-postcheck-client-20260503041603` 首次写入 2 条事件，重复请求返回 0 条新增、2 条重复
- Browser E2E: `http://192.144.187.174/zh/admin/agent-ops` 的行为轨迹页显示 post-check `命令执行` / `工具调用`，gateway 模型事件显示为中文 `模型请求`

---

### 2026-05-03 Roome payment test amount

roome her-web 测试库 `her-web-roome-db-clone` 临时把支付测试金额配置为 1 分，方便用真实支付通道验证回调和订单副作用：

- `stripe_test_amount=1`
- `creem_test_amount=1`
- `paypal_test_amount=1`
- `alipay_test_amount=1`
- `wechat_test_amount=1`

只改 roome clone DB，不改生产库；随后 `docker service update --force her-web-roome-test` 清掉 her-web 进程内 1 小时 config cache。回滚前配置备份在服务器 `/home/ubuntu/roome-test/web-payment-test-amount-config-before-20260503114458.tsv`。

---

### 2026-05-03 Roome pricing renewal follow-up

roome 测试 her-web 已部署：

- web: `her-web:roome-test-20260503134151`

本轮只更新 roome 测试服务，生产 `her-herweb-a8y5ka` 未变更，仍为 `sha256:67e569e4acffcf55c617eba9d02f1edfd06f4b85d783bb053e632253ddaf9ec0`。

变更：

- Max 月付文案从 `国际模型` 调整为 `顶尖模型`。
- 支付渠道商品信息统一为 `Her 订阅 - <套餐> - 订单号 <orderNo>`。
- Active Max：Pro 卡不可买，Max 卡可续费。
- Active Pro：同周期显示续费，长周期显示补差价升级；短周期仍不可买。
- 被禁用的购买按钮使用黑色按钮样式，不再在推荐卡上显示成红/粉色。

---

### 2026-05-03 Roome admin finance detail follow-up

roome 测试 her-web 已部署：

- web: `her-web:roome-test-20260503140942`

本轮只更新 roome 测试服务，生产 `her-herweb-a8y5ka` 未变更，仍为 `sha256:67e569e4acffcf55c617eba9d02f1edfd06f4b85d783bb053e632253ddaf9ec0`。

变更：

- 首页/营销页右上角 `价格`、`登录/注册`、`EN` 改为同组等间距按钮。
- 邀请码页 `新建邀请码` 按钮移动到标题说明下方。
- 管理员财务详情优先显示付费订阅有效期，不再被邀请码试用到期覆盖。
- 管理员财务详情把同一笔付款对应的订阅副作用合并进付款行，余额金额显示 `+/-`。
- 管理员用户详情的当前权限摘要去掉 `模型区域` 块。

### 2026-05-11 · HerClub 支付流程优化 + 数据修复

> 摘要：两次生产部署（d9a9e8a + aa02747）+ 线上数据修复。

**代码部署（两轮）**：

1. `d9a9e8a` — #73 QR 弹窗居中 + #74 恢复订单走 form + #75 购买资格检查 + footer 备案号
2. `aa02747` — QR 弹窗加「重新下单」按钮（关闭旧订单重来）

**线上数据修复（3 轮）**：

第 1 轮：邀请码来源修正
- 4 个 basic 朋友邀请码 `HCLUB-` → `HER-` + `is_hclub = false`（27375278/303128886/55123436/85531553）
- 4 个用户 `her_club_source` → NULL
- 翦（70045155）+ 水良子（993570085）补 `her_club_tier = max, her_club_source = invite_code`

第 2 轮：清零测试购买
- 4 张会员卡 disable（club-0001~0004：slysasnf/1711267233/shuiliangzi727/1@test.com）
- 5 个订单 closed
- 4 个用户 `her_club_source` → NULL
- **遗漏**：没清 `her_club_tier` → 徽章又出现了

第 3 轮：补清 tier
- 6 个用户 `her_club_tier` → NULL（4 个测试购买 + 翦 + 水良子）
- 4 个 basic 朋友用户 `her_club_tier` → NULL（改 source 后徽章规则反转导致显示）

**最终状态**：非 invite_code 用户有 tier 的 = 0；invite_code 用户保留 tier = 25；活跃会员 = 0

**回滚**：`rollback.sh` 可回到 `d9a9e8a` 或前一版本；数据修复需手动反向 SQL。昨天的备份表 `_backup_herclub_*_20260510` 仍在服务器上。

**踩坑**：
- `release.sh` report token 含时间戳，每次 re-check 重新生成导致 ACCEPT 永远不匹配 → 用 `deploy.sh` legacy direct 模式绕过
- 工作区有 skip-worktree 文件 + 其他任务 WIP 未跟踪测试文件 → 需要 stash + 移走才能通过 preflight
- **tier + source 必须成对操作**：徽章显示依赖 `tier 非空 AND source !== 'invite_code'`。单独清 source 不清 tier 会导致徽章反转出现；单独清 tier 但不清 source 会留残余数据。裸 SQL `UPDATE herclub_member SET status = 'disabled'` 不会触发 `disableMember()` 函数的 tier 自动清理逻辑。以后清会员数据必须走函数或者同时清 tier+source+granted 全套字段。
